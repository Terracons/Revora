# Comma-separated list of origins allowed to call the API
CORS_ORIGINS=http://localhost:5173
DATABASE_URL=sqlite:///./revora.db

# Email notifications (Resend) for new leads — leave RESEND_API_KEY unset to disable
RESEND_API_KEY=
NOTIFY_EMAIL=
FROM_EMAIL=Revora <onboarding@resend.dev>
